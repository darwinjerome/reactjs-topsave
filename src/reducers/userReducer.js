//import { GET_USER_FBAUTH } from '../actions/types';

// this is where you initialised application level states and variables
const initialState = {
  //data: []
  
}

export default function(state = initialState, action){

  switch(action.type){
    default:
      return state;
    
  }
}